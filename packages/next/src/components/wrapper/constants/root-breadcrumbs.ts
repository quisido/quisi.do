import type Breadcrumb from '../../../types/breadcrumb';

const ROOT_BREADCRUMBS: readonly Breadcrumb[] = Object.freeze([
  {
    children: 'quisi.do',
    path: '/',
  },
]);

export default ROOT_BREADCRUMBS;
