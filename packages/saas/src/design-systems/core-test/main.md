- The component exposes the content directly related to the document's central
  topic through the `main` landmark role[^1].
- A page has no more than one main landmark for each containing `document` or
  `application`[^1].

[^1]: https://w3c.github.io/aria/#main
