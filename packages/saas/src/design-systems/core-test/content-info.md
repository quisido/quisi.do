- The component exposes information about its parent document through the
  `contentinfo` landmark role[^1].
- A page has no more than one content-information landmark for each containing
  `document` or `application`[^1].

[^1]: https://w3c.github.io/aria/#contentinfo
