- The component exposes a mathematical expression through the `math` role[^1].
- Native MathML remains available to the browser and assistive technology[^1].
- Textual representations such as TeX can be exposed directly or embedded as
  annotations for a polyfill[^1].
- When native math is unavailable, a spoken label or image text alternative
  provides an accessible fallback[^1].

[^1]: https://w3c.github.io/aria/#math
