- Give each region a brief label that describes the purpose of the content in
  the region[^1].
- Reference a visible label with `aria-labelledby` if a visible label is
  present. Include the label inside of a heading whenever possible[^1].

[^1]: https://w3c.github.io/aria/#region
