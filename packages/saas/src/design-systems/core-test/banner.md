- The component exposes site-oriented content as a `banner` landmark[^1].
- A page has no more than one banner for each containing `document` or
  `application`[^1].

[^1]: https://w3c.github.io/aria/#banner
