- A status does not receive focus as a result of change in status[^1].
- Statuses have an implicit `aria-live` value of `'polite'` and an implicit
  `aria-atomic` value of `true`[^1].

[^1]: https://w3c.github.io/aria/#status
