- The component exposes a section containing `listitem` children through the
  `list` role[^1].
- Unordered lists use native `ul` semantics and ordered lists use native `ol`
  semantics[^1].

[^1]: https://w3c.github.io/aria/#list
