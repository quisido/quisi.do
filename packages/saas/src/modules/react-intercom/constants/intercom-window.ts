import type IntercomWindow from '../types/intercom-window.js';

// Intercom requires its presence on the `window` object.
const INTERCOM_WINDOW: IntercomWindow = window;

export default INTERCOM_WINDOW;
