{
  "name": "@charlesstover/cloudflare-analytics-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler deploy src/index.ts --name cloudflare-analytics",
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --local",
    "prepack": "exit 0",
    "prepublish": "wrangler deploy --dry-run --outdir dist",
    "start": "yarn run dev",
    "tail": "wrangler tail --format pretty --ip self cloudflare-analytics"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "@types/jest": "^29.5.1",
    "@types/node": "^20.2.5",
    "jest": "^29.5.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.1"
  }
}
