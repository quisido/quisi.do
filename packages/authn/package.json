{
  "name": "@quisido/authn",
  "type": "module",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "attw": "attw",
    "delete": "wrangler d1 execute authn --file=./sql/delete.sql --local",
    "deploy": "wrangler deploy src/index.ts --name quisido-authn --outdir dist",
    "deploy:dry-run": "wrangler deploy src/index.ts --dry-run --name quisido-authn --outdir dist",
    "dev": "wrangler dev --local-protocol https",
    "init": "yarn run init:emails && yarn run init:oauth && yarn run init:users",
    "init:emails": "wrangler d1 execute authn --file=./sql/emails.sql --local",
    "init:oauth": "wrangler d1 execute authn --file=./sql/oauth.sql --local",
    "init:users": "wrangler d1 execute authn --file=./sql/users.sql --local",
    "jest": "jest",
    "jest:watch": "jest --watch",
    "login": "wrangler login",
    "prepack": "exit 0",
    "prepublish": "exit 0",
    "tail": "wrangler tail --format pretty --ip self authentication",
    "whoami": "wrangler whoami"
  },
  "dependencies": {
    "cookie": "^0.6.0",
    "form-urlencoded": "^2.0.4",
    "jsonapi-datastore": "^0.4.0-beta",
    "patreon": "^0.4.1",
    "unknown2string": "workspace:^"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.13.2",
    "@cloudflare/workers-types": "^4.20231121.0",
    "@types/cookie": "^0",
    "@types/jest": "^29.5.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.2",
    "wrangler": "^3.17.1"
  }
}
