# yaml-language-server: $schema=../../../schemas/github-action.jsonc
name: Lighthouse
description: Runs Lighthouse audits

runs:
  using: composite
  steps:
    - name: Setup
      uses: ./.github/actions/setup
    - name: Audit
      shell: bash
      working-directory: packages/saas
      run: |
        chmod +x ./scripts/lighthouse-production.sh;
        ./scripts/lighthouse-production.sh;
    - name: Validate
      run: node ./scripts/postlighthouse/index.js
      shell: bash
      working-directory: packages/saas
    - name: Upload
      if: always()
      uses: actions/upload-artifact@v6
      with:
        name: lighthouse-production
        if-no-files-found: error
        path: |
          packages/saas/lighthouse.report.*
          packages/saas/lighthouse-*.devtoolslog.json
          packages/saas/lighthouse-*.trace.json
