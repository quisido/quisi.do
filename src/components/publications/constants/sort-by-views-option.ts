import { SelectProps } from '@awsui/components-react/select';
import Sort from '../constants/sort';

const SORT_BY_VIEWS_OPTION: SelectProps.Option = {
  label: 'Views',
  value: Sort.Views,
};

export default SORT_BY_VIEWS_OPTION;
