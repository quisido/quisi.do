import { withStyles } from '@material-ui/core';

export default withStyles({
  root: {
    paddingBottom: '1em'
  },
  source: {
    margin: '0 auto',
    maxWidth: 610,
    paddingTop: '1em',
    textAlign: 'right'
  }
});
