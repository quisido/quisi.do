import { withStyles } from '@material-ui/core';

export default withStyles({
  root: {
    cursor: 'pointer',
    textAlign: 'center',
  },
  source: {
    margin: '0 auto',
    maxWidth: 610,
    paddingTop: '1em',
    textAlign: 'right',
  },
});
