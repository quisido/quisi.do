import { withStyles } from '@material-ui/core';

export default withStyles({
  root: {
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'column'
  },
  viewMore: {
    margin: '1em'
  }
});
