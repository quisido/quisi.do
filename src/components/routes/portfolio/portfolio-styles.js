import { withStyles } from '@material-ui/core';

export default withStyles({
  backToTop: {
    display: 'block',
    margin: '0 auto',
    width: 'fit-content'
  },
  root: {
    marginBottom: '2em'
  },
  sections: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    margin: '1em 0.5em',
    maxWidth: '100%',
  }
});
