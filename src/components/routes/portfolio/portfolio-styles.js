import { withStyles } from '@material-ui/core';

export default withStyles({
  backToTop: {
    display: 'block',
    margin: '1em auto',
    width: 'fit-content'
  },
  root: {
    marginBottom: '2em'
  }
});
