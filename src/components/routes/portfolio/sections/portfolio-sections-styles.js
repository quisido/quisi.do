import { withStyles } from '@material-ui/core';

export default withStyles({
  root: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    margin: '1em 0.5em',
    maxWidth: '100%',
  }
});
