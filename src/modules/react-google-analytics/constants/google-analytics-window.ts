import type GoogleAnalyticsWindow from '../types/google-analytics-window';

// Google Analytics requires its presence on the `window` object.
const GOOGLE_ANALYTICS_WINDOW: GoogleAnalyticsWindow = window;

export default GOOGLE_ANALYTICS_WINDOW;
